// Twitter/X unfurls read `twitter:image` separately from `og:image` — reuse
// the exact same generated card rather than duplicating the render.
export { default, alt, size, contentType } from "./opengraph-image";
